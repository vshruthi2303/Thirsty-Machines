# Thirsty Machines

Static site. No build step.

## Deploy

1. Create a new GitHub repo and push the contents of this folder (`index.html`, `support.js`) to the repo root.
2. On Vercel: **Add New → Project → Import** the repo.
3. Framework Preset: **Other**. Leave Build Command empty. Output Directory: `./`
4. Deploy.

Any push to `main` redeploys automatically.
